syntax = "proto3";

option go_package = "/pb";

service SendMessage {
  rpc RequestMessage (Request) returns (Response);
}

message Request {
  string Message = 1;
}

message Response {
  int32 status = 1;
}
